DIAGNÓSTICO DEL ERROR DE CLOUDINARY:

El error "Upload preset not found" que aparece en la consola indica que Cloudinary no reconoce el preset proporcionado ('4x4661tTevWcpscPpH8wOaYDhrI') para tu cuenta ('dbbjxhvz5').

Esto suele deberse a dos razones principales:
1. El Preset no existe en tu configuración de Cloudinary.
2. El Preset está configurado como "Signed" (Firmado). Para subir archivos directamente desde el navegador (como lo hace admintest2.html) sin un servidor de por medio, el preset DEBE estar configurado como "Unsigned" (No firmado) en el panel de Cloudinary (Settings -> Upload -> Upload presets).

SOLUCIÓN:
Ve a tu consola de Cloudinary, busca el preset '4x4661tTevWcpscPpH8wOaYDhrI' y asegúrate de que el "Signing Mode" sea "Unsigned". Si no existe, créalo con ese nombre y asegúrate de que sea "Unsigned".

---------------------------------------------------------
SQL PARA SUPABASE:

Si aún no has creado la tabla necesaria en Supabase, ejecuta el siguiente código SQL en el editor de consultas (SQL Editor) de tu proyecto:

CREATE TABLE IF NOT EXISTS render3d_textures (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    gltf_key TEXT NOT NULL,         -- 'flyer', 'poster', 'sticker', 'tableqr', etc.
    label TEXT NOT NULL,            -- Nombre del diseño
    texture_url TEXT NOT NULL,      -- URL de Cloudinary
    created_at TIMESTAMPTZ DEFAULT now()
);

-- Desactivar RLS para permitir inserciones (ajustar según necesidades de seguridad)
ALTER TABLE render3d_textures DISABLE ROW LEVEL SECURITY;
