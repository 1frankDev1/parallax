DIAGNÓSTICO DEL ERROR DE CLOUDINARY:

El error "Upload preset not found" que aparecía en la consola indicaba que Cloudinary no reconocía el preset proporcionado anteriormente. Basado en la nueva información, se ha actualizado el sistema para utilizar el preset correcto: 'Tragalero'.

SOLUCIÓN APLICADA:
1. Se ha actualizado el preset de subida a 'Tragalero' en:
   - docs/admintest2.html (Administración de Texturas 3D)
   - docs/configdirectorio.html (Configuración de Restaurantes)
2. Se ha verificado que el preset esté configurado como "Unsigned" en Cloudinary para permitir la subida directa desde el navegador.

---------------------------------------------------------
SQL PARA SUPABASE:

Si aún no has creado la tabla necesaria en Supabase para las texturas 3D, ejecuta el siguiente código SQL en el editor de consultas (SQL Editor) de tu proyecto:

CREATE TABLE IF NOT EXISTS render3d_textures (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    gltf_key TEXT NOT NULL,         -- 'flyer', 'poster', 'sticker', 'tableqr', etc.
    label TEXT NOT NULL,            -- Nombre del diseño
    texture_url TEXT NOT NULL,      -- URL de Cloudinary
    created_at TIMESTAMPTZ DEFAULT now()
);

-- Habilitar permisos si es necesario
ALTER TABLE render3d_textures DISABLE ROW LEVEL SECURITY;
